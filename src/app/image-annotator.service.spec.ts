import { ImageAnnotatorService } from './image-annotator.service';

describe('ImageAnnotator', () => {
  it('should create an instance', () => {
    expect(new ImageAnnotatorService()).toBeTruthy();
  });
});
